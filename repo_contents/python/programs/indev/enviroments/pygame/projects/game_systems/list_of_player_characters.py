def get_player_character_data(name):
    #if name == "testCharacter":
        #from testCharacter_player_character_data import player_character_data
        #return player_character_data()
	if name == 'Harry':
		from Harry_player_character_data import player_character_data
		return player_character_data()
	if name == 'Harrison':
		from Harrison_player_character_data import player_character_data
		return player_character_data()
	if name == 'Bill':
		from Bill_player_character_data import player_character_data
		return player_character_data()
